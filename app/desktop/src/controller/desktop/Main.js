/**
 * @class Bidmo.controller.desktop.main
 * @extends Bidmo.controller.Main
 *
 * This is the Main controller subclass for the 'desktop' profile.
 *
 * The table profile differs from the phone profile in that navigation is done via screens full of icons
 * instead of a nested list.
 */
Ext.define('Bidmo.controller.desktop.Main', {
    extend: 'Bidmo.controller.common.Main',
});